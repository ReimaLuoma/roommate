version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd roommate
        - mkdir build
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
